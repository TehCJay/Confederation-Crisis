# I am creating this file to place triggers that seemly are truly ours (not frankencode) but are missing or broken.
# If you see a trigger here with "always = yes" it is because when I found stuff using it, I had no idea what it was supposed to do.

may_create_duchy_trigger = {
    always = yes
}

may_create_kingdom_trigger = {
    always = yes
}

may_create_empire_trigger = {
    always = yes
}
